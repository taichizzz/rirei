# Implementation notes

- Use only JavaScript/Node built-ins and export from `src/index.js`.
- Determinism and validation are part of the API, not optional cleanup.
- Do not mutate input event objects or dependency arrays. Run `npm test` after edits.
